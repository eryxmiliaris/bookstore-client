import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#8B5CF6", // Change this to your desired color
    },
  },
});

export default theme;
